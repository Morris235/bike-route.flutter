import 'package:flutter/material.dart';

// 로그인이 필요할 때마다 호출되는 모달
class LoginModal extends StatelessWidget {
  const LoginModal({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}